@model CRM.Domain.Entities.Student

@{
    ViewData["Title"] = "Create Student";
    ViewData["Action"] = "Create";
}

@{await Html.RenderPartialAsync("_StudentsPartial", Model, ViewData);}

@section Scripts
{
    <script>
        $("#crm-students").addClass('active');
    </script>
    @Html.Raw(CRM.WebUI.ViewComponents.CodesListViewComponent.Register())
}
